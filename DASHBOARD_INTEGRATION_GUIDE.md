# راهنمای جامع و پرامپت‌های اتصال پیشخوان (Dashboard) به آورا وایب (AuraVibe)

این سند راهنمای کامل، گام‌به‌گام و فنی جهت متصل کردن پیشخوان (داشبورد مدیریتی) به فروشگاه اینترنتی **AuraVibe** است.

---

## ۱. معماری کلی سیستم (Architecture Overview)

سیستم از ۳ لایه اصلی تشکیل می‌شود:
1. **فرانت‌اند فروشگاه (AuraVibe Storefront):** برنامه فعلی مبتنی بر React + TypeScript + Tailwind CSS.
2. **پیشخوان مدیریتی (Admin Dashboard):** داشبورد قبلی شما که برای مدیریت محصولات، سفارش‌ها، کاربران و مقالات استفاده می‌شود.
3. **بک‌اند و پایگاه داده (Backend & Database API):** لایه واسط که داده‌ها را ذخیره کرده و به فرانت‌اند و پیشخوان ارائه می‌دهد.

### پیشنهادات برای لایه بک‌اند (بدون نیاز به کدنویسی سنگین):
- **گزینه ۱ (پیشنهادی - بدون کدنویسی بک‌اند): Supabase یا PocketBase**
  - ابزارهای BaaS (Backend-as-a-Service) که دیتابیس PostgreSQL/SQLite و APIهای REST/GraphQL همراه با سیستم مدیریت کاربران و فایل‌ها (برای آپلود تصاویر) را به‌صورت آماده و بدون نیاز به نوشتن کد بک‌اند ارائه می‌دهند.
- **گزینه ۲: Node.js (Express / NestJS) + Prisma + PostgreSQL**
  - یک بک‌اند ساختاریافته ساده با Express.js.

---

## ۲. مدل‌ها و اسکیما دیتابیس (Database Schema)

برای هماهنگی کامل فرانت‌اند آورا وایب و پیشخوان، جداول دیتابیس باید به شرح زیر تعریف شوند:

### ۱. جدول محصولات (`products`)
| فیلد | نوع داده | توضیحات |
| :--- | :--- | :--- |
| `id` | BigInt / UUID | شناسه یکتا |
| `name` | String | نام محصول (مثلاً: گوشواره مدل آورا کد ۱۰۱) |
| `category` | String | نام دسته‌بندی (مثلاً: گردنبند، ساعت، اسکرانچی، کلیپس) |
| `price` | Integer | قیمت اصلی به تومان |
| `old_price` | Integer (Nullable) | قیمت قبلی (در صورت وجود تخفیف) |
| `image_url` | String | لینک تصویر محصول |
| `badge` | String (Nullable) | برچسب (جدید / تخفیف ویژه / پرفروش) |
| `colors` | Array/JSON | لیست کدهای رنگ (مثلاً `["#37192C", "#FFF3C5"]`) |
| `description` | Text | توضیحات کامل محصول |
| `stock` | Integer | موجودی انبار |
| `created_at` | Timestamp | تاریخ ثبت |

### ۲. جدول سفارش‌ها (`orders`)
| فیلد | نوع داده | توضیحات |
| :--- | :--- | :--- |
| `id` | String | کد سفارش (مثلاً `ORD-9821`) |
| `user_id` | BigInt / UUID | شناسه کاربر ثبت کننده |
| `customer_name` | String | نام و نام خانوادگی خریدار |
| `customer_phone` | String | شماره همراه |
| `shipping_address` | JSON / Text | شهر، منطقه، محله و آدرس دقیق |
| `total_price` | Integer | مبلغ کل به تومان |
| `status` | Enum | وضعیت (`در حال پردازش`, `ارسال شده`, `تحویل داده شده`, `لغو شده`) |
| `items` | JSON | لیست کالاها شامل id، نام، قیمت، تعداد و رنگ انتخاب شده |
| `shipping_method` | String | روش ارسال (پست پیشتاز / پیک) |
| `created_at` | Timestamp | تاریخ ثبت سفارش |

### ۳. جدول کاربران (`users`)
| فیلد | نوع داده | توضیحات |
| :--- | :--- | :--- |
| `id` | BigInt / UUID | شناسه کاربر |
| `name` | String | نام کاربر |
| `username` | String | نام کاربری |
| `phone` | String | شماره همراه |
| `avatar_url` | String (Nullable) | تصویر پروفایل |
| `city` | String | شهر |
| `district` | String | منطقه |
| `neighborhood` | String | محله |
| `address_detail` | Text | آدرس تکمیلی |

### ۴. جدول مقالات مجله (`articles`)
| فیلد | نوع داده | توضیحات |
| :--- | :--- | :--- |
| `id` | BigInt / UUID | شناسه مقاله |
| `title` | String | عنوان مقاله |
| `digest` | Text | چکیده و خلاصه |
| `content` | Text | متن کامل مقاله |
| `tag` | String | برچسب (مثلاً: ترند فصل، راهنمای استایل) |
| `image_url` | String | تصویر کاور مقاله |
| `created_at` | Timestamp | تاریخ انتشار |

---

## ۳. لیست API Endpointهای مورد نیاز (API Contract)

### محصولات (Products API)
- `GET /api/products` -> دریافت لیست همه محصولات (با قابلیت فیلتر بر اساس دسته و قیمت)
- `GET /api/products/:id` -> دریافت جزئیات یک محصول
- `POST /api/products` -> ایجاد محصول جدید (توسط پیشخوان)
- `PUT /api/products/:id` -> ویرایش محصول (توسط پیشخوان)
- `DELETE /api/products/:id` -> حذف محصول (توسط پیشخوان)

### سفارش‌ها (Orders API)
- `GET /api/orders` -> دریافت لیست سفارش‌ها جهت نمایش در پیشخوان
- `POST /api/orders` -> ثبت سفارش جدید از فرانت‌اند آورا وایب
- `PATCH /api/orders/:id/status` -> تغییر وضعیت سفارش (مثلاً از "در حال پردازش" به "ارسال شده") در پیشخوان

### کاربران و احراز هویت (Auth & Users API)
- `POST /api/auth/send-otp` -> ارسال کد تایید اس‌ام‌اس
- `POST /api/auth/verify-otp` -> تایید کد و ورود/ثبت‌نام
- `GET /api/users/profile` -> دریافت پروفایل
- `PUT /api/users/profile` -> به‌روزرسانی آدرس و اطلاعات کاربر

---

## ۴. پرامپت‌های آماده (Prompt Templates) جهت ارائه به هوش مصنوعی

می‌توانید پرامپت‌های زیر را عیناً کپی کرده و برای ساخت یا تغییر پیشخوان و متصل کردن آن استفاده کنید:

### 🎯 پرامپت شماره ۱: ساخت یا تنظیم پیشخوان مدیریتی (Admin Dashboard Integration)
```text
سلام، من یک سایت فروشگاهی به نام AuraVibe با React و TypeScript دارم و می‌خواهم پیشخوان مدیریتی فعلی‌ام را به داده‌های این فروشگاه متصل کنم.

مشخصات داده‌های AuraVibe به این شرح است:
۱. محصولات (Products): شامل id, name, category, price, oldPrice, image, badge, colors, description, stock.
۲. سفارش‌ها (Orders): شامل id, customerName, phone, address (city, district, neighborhood, addressDetail), price, status (در حال پردازش | ارسال شده | تحویل داده شده | لغو شده), items, shippingMethod.
۳. مقالات مجله (Articles): شامل id, title, digest, date, image, tag.

لطفاً پیشخوان مدیریتی من را طوری تنظیم کن که:
۱. امکان مشاهده، افزودن، ویرایش و حذف محصولات AuraVibe را داشته باشد (با قابلیت آپلود عکس).
۲. جدول مدیریت سفارش‌ها را با قابلیت تغییر وضعیت سفارش (مثلاً تغییر به "ارسال شده") نشان دهد.
۳. بخش مدیریت مقالات مجله استایل آورا را داشته باشد.
۴. فرم‌ها و جداول کاملاً راست‌چین (RTL) و با تقویم شمسی/فارسی باشند.
```

### 🎯 پرامپت شماره ۲: ایجاد بک‌اند ساده با Node.js/Express و Prisma
```text
سلام، من ناآشنا با بک‌اند هستم. لطفاً یک پروژه ساده Node.js با Express.js و Prisma ORM به زبان TypeScript برای سیستم AuraVibe برایم ایجاد کن.

نیازها:
۱. مدل‌های دیتابیس (Prisma Schema) برای Products, Orders, Users و Articles بر اساس داده‌های AuraVibe.
۲. روت‌های کامل CRUD برای محصولات (`/api/products`).
۳. روت ثبت سفارش برای مشتریان و روت دریافت/ویرایش وضعیت سفارش‌ها برای پیشخوان (`/api/orders`).
۴. تنظیمات CORS برای ارتباط بدون مشکل فرانت‌اند (پورت ۵۱۷۳) و پیشخوان (پورت ۳۰۰۰).
۵. یک راهنمای قدم‌به‌قدم بسیار ساده برای اجرا روی سیستم لوکال با دستورات npm run dev.
```

### 🎯 پرامپت شماره ۳: متصل کردن فرانت‌اند AuraVibe به API واقعی
```text
سلام، من فروشگاه AuraVibe را دارم و می‌خواهم داده‌های ساختگی (mock data) موجود در src/data.ts را با فراخوانی APIهای واقعی سرویس بک‌اند جایگزین کنم.

لطفاً:
۱. یک فایل `src/services/api.ts` ایجاد کن که با استفاده از fetch یا axios به آدرس `http://localhost:5000/api` متصل شود.
۲. توابع `getProducts()`, `getProductById(id)`, `createOrder(orderData)`, `getArticles()` را بنویس.
۳. در App.tsx متغیرهای حالت محصولات و سفارش‌ها را به جای داده‌های ثابت از این توابع API مقداردهی و به‌روزرسانی کن.
```

---

## ۵. جمع‌بندی و گام‌های بعدی

۱. **انتخاب بک‌اند:** استفاده از **Supabase** عالی‌ترین گزینه است زیرا نیازی به کدنویسی بک‌اند ندارد و دیتابیس، آپلود عکس و API را یکجا آماده می‌کند.
۲. **تنظیم پیشخوان:** استفاده از **پرامپت شماره ۱** جهت هماهنگ‌سازی فرم‌ها و جداول پیشخوان با کدهای AuraVibe.
۳. **اتصال فروشگاه:** استفاده از **پرامپت شماره ۳** جهت تعویض mock dataهای آورا وایب با API واقعی.
